// Atividade 10 — Pode entrar?
import { number, confirm } from '@inquirer/prompts';

// Regras da casa:
//   - maior de idade: idade >= 18
//   - entra quem tem ingresso E (é maior de idade OU está acompanhado)
//
// 1) Pergunte a idade (number), se tem ingresso (confirm) e se está acompanhado (confirm).
// 2) Guarde o resultado de cada regra em uma variável booleana (maior, entra).
// 3) Mostre os booleanos e, usando o operador ternário, a frase
//    "Entrada liberada" ou "Entrada barrada".
//
// Ainda não usamos if: use apenas os operadores >=, &&, || e ? :

// TODO: escreva seu código aqui
