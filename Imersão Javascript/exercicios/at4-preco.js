// Atividade 4 — Quatro formas de mostrar o mesmo número
const preco = 1499.9;
const desconto = 0.15;

// Mostre, cada um em uma linha:
// 1) o preço com 2 casas decimais usando toFixed
// 2) o preço no padrão brasileiro (toLocaleString com 'pt-BR')
// 3) o preço em reais (opções { style: 'currency', currency: 'BRL' })
// 4) o desconto em percentual (opções { style: 'percent' })
//
// Atenção: toFixed devolve TEXTO. Confira com typeof.

// TODO: escreva seu código aqui
